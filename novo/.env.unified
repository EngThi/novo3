# ====================================================================
# PIPELINE UNIFICADO v3.0 - CONFIGURAÇÃO SIMPLIFICADA
# ====================================================================
# Uma única configuração que funciona para todos os modos:
# FREE, PREMIUM, PREMIUM-V2

# === MODO DE OPERAÇÃO ===
# auto = Detecção automática baseada em APIs disponíveis
# free = Apenas serviços gratuitos
# premium = APIs pagas + qualidade alta
# premium-v2 = Gemini 2.5 Flash + Nano Banana (melhor qualidade)
PIPELINE_MODE=auto

# === GEMINI APIs (OBRIGATÓRIA) ===
# Pelo menos uma chave é necessária
GEMINI_API_KEY=sua_chave_gemini_principal

# Chaves adicionais para rotação (OPCIONAL)
GEMINI_API_KEY_2=sua_chave_gemini_2
GEMINI_API_KEY_3=sua_chave_gemini_3

# === TTS PREMIUM (OPCIONAL) ===
# Para qualidade de voz superior
# Gemini 2.5 Flash TTS (Recomendado)
GEMINI_IMAGE_API_KEY=sua_chave_gemini_image

# Hugging Face (Alternativo)
HUGGINGFACE_API_KEY=sua_chave_huggingface

# === GERAÇÃO DE IMAGENS PREMIUM (OPCIONAL) ===
# Nano Banana para qualidade estado da arte
NANO_BANANA_API_KEY=sua_chave_fal_ai
NANO_BANANA_FAL_KEY=sua_chave_fal_ai_alternativa

# === CONFIGURAÇÕES DE VOZ ===
# Voz principal (30+ opções disponíveis)
TTS_VOICE_PRIMARY=Kore

# Voz alternativa
TTS_VOICE_SECONDARY=Zephyr

# Seleção automática por tipo de conteúdo
TTS_AUTO_SELECT=true

# Idioma padrão
DEFAULT_TTS_LANGUAGE=pt-BR

# === CONTEÚDO E QUALIDADE ===
# Categoria padrão
DEFAULT_CATEGORY=misterios-brasileiros

# Qualidade de vídeo
DEFAULT_VIDEO_QUALITY=1080p

# Duração ideal (segundos)
IDEAL_VIDEO_DURATION=180

# === OTIMIZAÇÕES ===
# Habilitar cache inteligente
CACHE_ENABLED=true

# Tamanho máximo do cache (MB)
MAX_CACHE_SIZE=512

# Limpeza automática
AUTO_CLEANUP=true

# Logs detalhados
VERBOSE_LOGGING=true

# === NOTIFICAÇÕES (OPCIONAL) ===
# Discord webhook
DISCORD_WEBHOOK_URL=sua_url_webhook_discord

# === GOOGLE SERVICES (OPCIONAL) ===
# Para tracking em planilhas
GOOGLE_DRIVE_REFRESH_TOKEN=seu_refresh_token
GOOGLE_SHEET_ID=id_da_sua_planilha

# === SEGURANÇA ===
# Seed para criptografia (IMPORTANTE)
ENCRYPTION_SEED=sua_seed_super_secreta

# ====================================================================
# GUIA RÁPIDO DE USO
# ====================================================================

# 1. CONFIGURAÇÃO MÍNIMA (Apenas 1 linha!):
#    GEMINI_API_KEY=sua_chave

# 2. USO SIMPLES:
#    npm run start          # Modo automático
#    npm run free           # Apenas gratuito
#    npm run premium        # Qualidade alta
#    npm run premium-v2     # Melhor qualidade

# 3. PERSONALIZAÇÃO:
#    npm run voice          # Escolher voz
#    node pipeline-unified.js --voice=Zephyr

# ====================================================================
# DETECÇÃO AUTOMÁTICA DE MODO
# ====================================================================
# O sistema detecta automaticamente suas capacidades:

# MODO FREE:
# - Apenas GEMINI_API_KEY configurada
# - Usa Pollinations (imagens) + gTTS/eSpeak (voz)

# MODO PREMIUM:
# - GEMINI + HUGGINGFACE_API_KEY
# - Usa Hugging Face models + qualidade alta

# MODO PREMIUM-V2:
# - GEMINI + NANO_BANANA + HUGGINGFACE
# - Máxima qualidade: Gemini TTS + Nano Banana images

# ====================================================================
# ESTIMATIVA DE CUSTOS
# ====================================================================

# MODO FREE: $0.05-0.08/vídeo
# - Gemini 2.5 Flash: $0.05
# - Serviços gratuitos: $0.00

# MODO PREMIUM: $0.10-0.15/vídeo  
# - Gemini + Hugging Face: $0.10
# - APIs premium ocasionais: $0.05

# MODO PREMIUM-V2: $0.20-0.35/vídeo
# - Gemini 2.5 Flash TTS: $0.08
# - Nano Banana images: $0.20 (5 imagens)
# - Qualidade máxima garantida

# ====================================================================
# TROUBLESHOOTING
# ====================================================================

# Problema: "Nenhuma API disponível"
# Solução: Verificar GEMINI_API_KEY

# Problema: "Modo não detectado"
# Solução: node pipeline-unified.js --debug

# Problema: "Qualidade baixa"
# Solução: Adicionar mais API keys premium

# Ver logs: npm run logs
# Limpar cache: npm run cleanup
# Ver estatísticas: npm run stats

# ====================================================================